Module: mhi.pscad.graph
============================================================


Class: Curve
  A Curve on a graph

  Method: active(fget)
    The index of the active curve trace

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: domain(self) -> 'array.array'
    The domain of the curve.

Returns:
    array.array('d'): the domain axis values

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_parameters(self)
    

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: parameters(self, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str')
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: set_parameters(self, **parameters)
    

  Method: source(fget)
    The source of the curve (Read-only)

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_end(self) -> 'None'
    Put last in the list of curves in the graph.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: to_start(self) -> 'None'
    Put first in the list of curves in the graph.

  Method: trace(self, trace_num: 'int' = 0) -> 'array.array'
    An array of sample values for the indexed trace in the curve.

Parameters:
    trace_num (int): The trace index, starting at 0

Returns:
    array.array('d'): the trace's sample values

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component


Class: FormCodec
  Form Parameter Coder/Decoder

  Method: application(remote)
    

  Method: bus(remote)
    

  Method: decode(self, parameters)
    Decode key-value dictionaries string values used to serialize
data in PSCAD's XML file format into dictionaries with
Human-Readable/Pythonic values

The dictionary keys are used to select the encoding method.

  Method: divider(remote)
    

  Method: encode(self, parameters)
    Encode key-value dictionaries with Human-Readable/Pythonic values into
key-value dictionaries with strings values used to serialize
data in PSCAD's XML file formats.

The dictionary keys are used to select the encoding method.

  Method: gfx_arc(remote)
    

  Method: gfx_line(remote)
    

  Method: gfx_oval(remote)
    

  Method: gfx_port(remote)
    

  Method: gfx_rect(remote)
    

  Method: gfx_shape(remote)
    

  Method: gfx_text(remote)
    

  Method: graph_frame(remote)
    

  Method: group_box(remote)
    

  Method: layer_options(remote)
    

  Method: overlay_graph(remote)
    

  Method: plot_frame(remote)
    

  Method: project(remote)
    

  Method: range(self, key: 'str') -> 'Any'
    Valid input for a given parameter

  Method: simset_external_task(remote)
    

  Method: simset_project_overrides(remote)
    

  Method: simset_project_task(remote)
    

  Method: simulation_set(remote)
    

  Method: sticky_note(remote)
    

  Method: user_canvas(remote)
    


Class: GraphFrame
  A container for holding one or more overlay graphs.

The Graph Frame parameters holds a set of properties
for the frame itself.

.. table:: Graph Frame Properties

   ============ ===== ============================================
   Param Name   Type  Description
   ============ ===== ============================================
   title        str   Caption of Graph Frame
   markers      bool  Show Markers
   lockmarkers  bool  Lock distance between X & O markers
   deltareadout bool  Show "Delta T" readout
   xmarker      float The X-Marker position
   omarker      float The O-Marker position
   pan_enable   bool  Auto Pan X-Axis enabled
   pan_amount   int   Auto Pan X-Axis percentage
   xtitle       str   Title of the x-axis
   xgrid        float X-Axis grid line spacing
   xfont        str   X-Axis font
   xangle       int   X-Axis label angle (in degrees)
   ============ ===== ============================================

  Method: add_overlay_graph(self, *sources: 'Union[Curve, PGB]') -> 'Tuple[OverlayGraph, List[Curve]]'
    Add a new Overlay Graph

Parameters:
    *sources: pgbs or curves to add to newly created graph (optional)

Returns:
    Tuple[OverlayGraph,List[Curve]]: The new Overlay Graph & its curves

  Method: add_poly_graph(self, *sources: 'Union[Curve, PGB]', digital: 'bool' = False) -> 'Tuple[PolyGraph, List[Curve]]'
    Add a new Stacked PolyGraph

Parameters:
    *sources: pgbs or curves to add to newly created graph (Optional)
    digital: Whether curve should be displayed as a digital signal.
        Defaults to ``False``. (Optional)


Returns:
    Tuple[PolyGraph,List[Curve]]: The new PolyGraph & its curves
    Newly added Stacked Polygraph

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: auto_sizing(fget)
    Enable/disable vertical auto sizing

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self, *graphs)
    Copy this component to the clipboard.

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self, *graphs)
    Cut this component to the clipboard

  Method: delete(self) -> 'bool'
    Delete this component.

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: follow_run(fget)
    Enable/disable auto pan (follow end of run)

  Method: get_location(self)
    Get this component's (x,y) location

Returns:
    Tuple[int, int]: The x,y location of the component, in grid units

  Method: get_parameters(self)
    

  Method: get_size(self) -> 'Tuple[int, int]'
    

  Method: glyphs(fget)
    Show/hide curve glyphs

  Method: grid(fget)
    Show/hide grid

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: markers(fget)
    Show/hide X/O markers

  Method: minimize(self, flag: 'bool' = True) -> 'None'
    Minimize the frame.

  Method: overlay_graph(self, iid)
    

  Method: pan(self, left: 'Optional[float]' = None, right: 'Optional[float]' = None, *, width: 'Optional[float]' = None) -> 'None'
    Set the location of the graph's panning bar.

  Method: panel(self, idx: 'int') -> 'GraphPanel'
    Retrieve the indexed panel contained in the frame.

Parameters:
    idx (int): Panel index (zero based)

  Method: panels(self) -> 'List[GraphPanel]'
    List of panels in contained in this frame

  Method: parameters(self, *, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: parent(fget)
    Canvas this component is located on (read-only)

:type: Canvas

  Method: paste(self)
    

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove(self, *panels: 'GraphPanel') -> 'None'
    Remove one or more graph panels from the Graph Frame

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: reset_x_axis(self)
    

  Method: reset_y_axis(self)
    

  Method: restore(self)
    Restore the frame from its minimized state.

  Method: set_auto_pan(self, enable: 'bool' = True) -> 'None'
    Set auto-pan on or off.

  Method: set_auto_sizing(self, enable: 'bool' = True) -> 'None'
    Set auto-sizing on or off.

  Method: set_location(self, x, y)
    Set the component's (x,y) location

Parameters:
    x (int): The new x location for this component
    y (int): The new y location for this component

  Method: set_parameters(self, **parameters)
    

  Method: set_size(self, width: 'int', height: 'int') -> 'None'
    

  Method: show_glyphs(self, show: 'bool' = True) -> 'None'
    Set the curve glyph visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the curve glyphs.

  Method: show_grid(self, show: 'bool' = True) -> 'None'
    Set the grid visibility

Parameters:
    show (bool): Set to ``False`` to turn off the grid.

  Method: show_markers(self, show: 'bool' = True) -> 'None'
    Set the marker visibility to on or off.

  Method: show_ticks(self, show: 'bool' = True) -> 'None'
    Set the tick visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the tick markers.

  Method: show_x_intercept(self, show: 'bool' = True)
    Set the X intercept visibility on or off

  Method: show_y_intercept(self, show: 'bool' = True) -> 'None'
    Set the Y intercept visibility on or off

  Method: size(fget)
    Set/get width & height of a sizeable component

  Method: synchronize_channel_limits(self) -> 'None'
    Set the limits of each channel in the frame to the limits of its graph.

  Method: ticks(fget)
    Show/hide axis tick marks

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: toggle_auto_pan(self) -> 'None'
    Toggle auto-pan on or off.

  Method: toggle_auto_sizing(self) -> 'None'
    Toggle auto-sizing on or off.

  Method: toggle_curve_glyphs(self) -> 'None'
    Toggle curve glyphs on or off

  Method: toggle_grid_lines(self) -> 'None'
    Toggle grid lines on or off

  Method: toggle_markers(self) -> 'None'
    Toggle the markers on or off.

  Method: toggle_minimize(self) -> 'None'
    Toggle minimized/restored state

  Method: toggle_tick_marks(self) -> 'None'
    Toggle tick marks on or off

  Method: toggle_x_intercept(self) -> 'None'
    Toggle X intercept on or off

  Method: toggle_y_intercept(self) -> 'None'
    Toggle Y intercept on or off

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component

  Method: x_intercept(fget)
    Show/hide x-intercept

  Method: y_intercept(fget)
    Show/hide y-intercept

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, *, compute_x_grid: 'bool' = True) -> 'None'
    Alter the graph's X-Axis extents

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Class: GraphPanel
  Graph Panel

Parent class of OverlayGraph, PolyGraph, ...

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self, *curves)
    

  Method: copy_data_all(self) -> 'None'
    Copy all data in the graph to the clipboard.

  Method: copy_data_between_markers(self) -> 'None'
    Copy data between the markers in the graph to the clipboard.

  Method: copy_data_visible(self) -> 'None'
    Copy visible data in the graph to the clipboard.

  Method: create_curve(self, source: 'Union[PGB, Curve]', index: 'int' = -1) -> 'Curve'
    Create a new curve on this graph from the given source.

If the source is another curve, that curve is cloned.

Parameters:
    source: The signal source (pgb or a curve)
    index: Position in graph (optional)

Returns:
    Curve: The created curve

  Method: create_curves(self, *sources: 'Union[PGB, Curve]') -> 'List[Curve]'
    Create new curves on this graph from the given sources.

If a source is another curve, that curve is cloned.

Parameters:
    *sources: The signal sources (pgbs or a curves)

Returns:
    List[Curve]: The created curves

  Method: crosshair(fget)
    Show / hide crosshair

  Method: curve(self, index: 'int') -> 'Curve'
    Retrieve a curve that belongs to this graph panel.

Parameters:
    index (int): The curve index, starting from 0

Returns:
    Curve: the indexed curve in this panel

  Method: curves(self) -> 'List[Curve]'
    Retrieve the curves that belong to this graph panel.

Returns:
    List[Curve]: the curves in this panel

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self, *curves)
    

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_parameters(self)
    

  Method: glyphs(fget)
    Show / hide curve glyphs

  Method: grid(fget)
    Show / hide grid

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: move_curve(self, curve: 'Curve', index: 'int' = -1)
    Move a curve to a given position on this graph.

If a curve is on a different graph, it is removed from that graph,
and added to this one.

Parameters:
    curve: The curve to move.
    index: Position in graph (Optional)

  Method: move_down(self) -> 'None'
    Move the graph one position down in the graph frame.

  Method: move_up(self) -> 'None'
    Move the graph one position up in the graph frame.

  Method: parameters(self, *, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: paste(self)
    Paste contents of the clipboard

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: set_parameters(self, **parameters)
    

  Method: show_crosshair(self, show: 'bool' = True) -> 'None'
    Set the crosshair visibility on or off.

  Method: ticks(fget)
    Show / hide axis tick marks

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_bottom(self) -> 'None'
    Put at the bottom of the graph frame.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: to_top(self) -> 'None'
    Put at the top of the graph frame.

  Method: toggle_crosshair(self) -> 'None'
    Toggle crosshair on or off.

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component

  Method: x_intercept(fget)
    Show / hide x-intercept

  Method: y_intercept(fget)
    Show / hide y-intercept

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, ymin: 'Optional[float]' = None, ymax: 'Optional[float]' = None, *, compute_x_grid: 'bool' = True, compute_y_grid: 'bool' = True) -> 'None'
    Set the horizontal and/or vertical limits of the overlay graph.

All parameters are optional.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit
    compute_x_grid (bool): Recompute x-grid spacing
    compute_x_grid (bool): Recompute y-grid spacing

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Class: MovableMixin
  Things that may be moved

  Method: copy(self) -> 'bool'
    Copy this component to the clipboard.

  Method: cut(self) -> 'bool'
    Cut this component to the clipboard

  Method: delete(self) -> 'bool'
    Delete this component.

  Method: get_location(self)
    Get this component's (x,y) location

Returns:
    Tuple[int, int]: The x,y location of the component, in grid units

  Method: parent(fget)
    Canvas this component is located on (read-only)

:type: Canvas

  Method: set_location(self, x, y)
    Set the component's (x,y) location

Parameters:
    x (int): The new x location for this component
    y (int): The new y location for this component


Class: OverlayGraph
  Overlay Graph

A graph object that may be contained in a graph frame.
Inherits from :class:`.GraphPanel`.

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self, *curves)
    

  Method: copy_data_all(self) -> 'None'
    Copy all data in the graph to the clipboard.

  Method: copy_data_between_markers(self) -> 'None'
    Copy data between the markers in the graph to the clipboard.

  Method: copy_data_visible(self) -> 'None'
    Copy visible data in the graph to the clipboard.

  Method: create_curve(self, source: 'Union[PGB, Curve]', index: 'int' = -1) -> 'Curve'
    Create a new curve on this graph from the given source.

If the source is another curve, that curve is cloned.

Parameters:
    source: The signal source (pgb or a curve)
    index: Position in graph (optional)

Returns:
    Curve: The created curve

  Method: create_curves(self, *sources: 'Union[PGB, Curve]') -> 'List[Curve]'
    Create new curves on this graph from the given sources.

If a source is another curve, that curve is cloned.

Parameters:
    *sources: The signal sources (pgbs or a curves)

Returns:
    List[Curve]: The created curves

  Method: crosshair(fget)
    Show / hide crosshair

  Method: curve(self, index: 'int') -> 'Curve'
    Retrieve a curve that belongs to this graph panel.

Parameters:
    index (int): The curve index, starting from 0

Returns:
    Curve: the indexed curve in this panel

  Method: curves(self) -> 'List[Curve]'
    Retrieve the curves that belong to this graph panel.

Returns:
    List[Curve]: the curves in this panel

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self, *curves)
    

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_parameters(self)
    

  Method: glyphs(fget)
    Show / hide curve glyphs

  Method: grid(fget)
    Show / hide grid

  Method: height(fget)
    Manual graph height

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: move_curve(self, curve: 'Curve', index: 'int' = -1)
    Move a curve to a given position on this graph.

If a curve is on a different graph, it is removed from that graph,
and added to this one.

Parameters:
    curve: The curve to move.
    index: Position in graph (Optional)

  Method: move_down(self) -> 'None'
    Move the graph one position down in the graph frame.

  Method: move_up(self) -> 'None'
    Move the graph one position up in the graph frame.

  Method: parameters(self, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Get/set Overlay Graph Settings

.. table:: Overlay Graph Settings

   ============== ====== ==============================================
   Param Name     Type   Description
   ============== ====== ==============================================
   glyphs         bool   Glyphs
   grid           bool   Grids
   grid_color     Color  Grid Line Colour
   curve_colours  Colors Curve Colour Pattern
   curve_colours2 Colors Curve Inverted Colour Pattern
   ticks          bool   Ticks
   yinter         bool   Y-Intercept
   xinter         bool   X-Intercept
   crosshair      bool   Crosshair
   manualscale    bool   Manual Scalling Only
   invertcolor    bool   Invert Colours
   title          Text   Y-Axis Title
   gridvalue      Real   Grid Size
   ymin           Real   Minimum Value
   ymax           Real   Maximum Value
   yintervalue    Real   Intercept Value
   autoframe      Real   Padding
   ============== ====== ==============================================

  Method: paste(self)
    Paste contents of the clipboard

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: set_parameters(self, **parameters)
    

  Method: set_zoom(self, xmin=None, xmax=None, ymin=None, ymax=None)
    Set the horizontal and vertical limits of the overlay graph.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit

  Method: show_crosshair(self, show: 'bool' = True) -> 'None'
    Set the crosshair visibility on or off.

  Method: show_glyphs(self, show: 'bool' = True) -> 'None'
    Set the curve glyph visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the curve glyphs.

  Method: show_grid(self, show: 'bool' = True) -> 'None'
    Set the grid visibility

Parameters:
    show (bool): Set to ``False`` to turn off the grid.

  Method: show_ticks(self, show: 'bool' = True) -> 'None'
    Set the tick visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the tick markers.

  Method: show_x_intercept(self, show: 'bool' = True)
    Set the X intercept visibility on or off

  Method: show_y_intercept(self, show: 'bool' = True) -> 'None'
    Set the Y intercept visibility on or off

  Method: synchronize_channel_limits(self) -> 'None'
    Set the limits of each channel to the limits of the graph.

  Method: ticks(fget)
    Show / hide axis tick marks

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_bottom(self) -> 'None'
    Put at the bottom of the graph frame.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: to_top(self) -> 'None'
    Put at the top of the graph frame.

  Method: toggle_crosshair(self) -> 'None'
    Toggle crosshair on or off.

  Method: toggle_curve_glyphs(self) -> 'None'
    Toggle curve glyphs on or off

  Method: toggle_grid_lines(self) -> 'None'
    Toggle grid lines on or off

  Method: toggle_tick_marks(self) -> 'None'
    Toggle tick marks on or off

  Method: toggle_x_intercept(self) -> 'None'
    Toggle X intercept on or off

  Method: toggle_y_intercept(self) -> 'None'
    Toggle Y intercept on or off

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component

  Method: x_intercept(fget)
    Show / hide x-intercept

  Method: y_intercept(fget)
    Show / hide y-intercept

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, ymin: 'Optional[float]' = None, ymax: 'Optional[float]' = None, *, compute_x_grid: 'bool' = True, compute_y_grid: 'bool' = True) -> 'None'
    Set the horizontal and/or vertical limits of the overlay graph.

All parameters are optional.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit
    compute_x_grid (bool): Recompute x-grid spacing
    compute_x_grid (bool): Recompute y-grid spacing

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Class: PanelMixin
  Things with multiple panels

  Method: panel(self, idx: 'int') -> 'GraphPanel'
    Retrieve the indexed panel contained in the frame.

Parameters:
    idx (int): Panel index (zero based)

  Method: panels(self) -> 'List[GraphPanel]'
    List of panels in contained in this frame


Class: PlotFrame
  X-Y Plot Frame

A Graph Frame that displays an X-Y curves where both X and Y values are
dependent on another quantity, such as time.

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: aperture(self, start: 'Optional[float]' = None, end: 'Optional[float]' = None)
    Set/Get the aperture limits of the XY Plot.

All parameters are optional.

Parameters:
    start (float): Aperture start
    end (float): Aperture end

Returns:
    Tuple[float, float]: The plot aperture

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self) -> 'bool'
    Copy this component to the clipboard.

  Method: copy_data_all(self) -> 'None'
    Copy all data in the XY plot to the clipboard.

  Method: copy_data_visible(self) -> 'None'
    Copy all data inside the XY plot's view aperature to the clipboard.

  Method: create_curve(self, x: 'Union[PGB, Curve, None]' = None, y: 'Union[PGB, Curve, None]' = None) -> 'Union[Curve, Tuple[Curve, Curve]]'
    Add a curve to the XY plot.

Parameters:
    x: The signal source for the X-axis (pgb or a curve)
    y: The signal source for the Y-axis (pgb or a curve)

Returns:
    Union[Curve, Tuple[Curve,Curve]]: The created curve(s)

Note:

    To plot a curve in an XY plot, both the X & Y signal sources
    are required.
    Using drag-and-drop, the X & Y signals sources are added separately,
    one at a time;
    to faciliate script recording, an ``x=`` or ``y=`` keyword argument
    is used to add the individual halves of the X & Y pairs separately.
    However, it is recommended to add both signal sources at once.

.. versionadded:: 2.1.1

  Method: create_curves(self, *xy: 'Tuple[Union[PGB, Curve], Union[PGB, Curve]]') -> 'List[Tuple[Curve, Curve]]'
    Add one or more curves to the XY plot.

Parameters:
    *xy (Tuple[X,Y]): Tuples of X/Y signal sources

Returns:
    List[Tuple[Curve,Curve]]: The created curve tuples

.. versionadded:: 2.1.1

  Method: crosshair(fget)
    Show/hide crosshair

  Method: curve(self, index: 'int') -> 'Tuple[Curve, Curve]'
    Retrieve a curve pair that belongs to this XY Plot.

Parameters:
    index (int): The curve pair index, starting from 0

Returns:
    Tuple[Curve,Curve]: the indexed curve pair in this plot

  Method: curves(self) -> 'List[Tuple[Curve, Curve]]'
    Retrieve the curves that belong to this XY Plot.

Returns:
    List[Tuple[Curve,Curve]]: the curve pairs in this plot

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self) -> 'bool'
    Cut this component to the clipboard

  Method: delete(self) -> 'bool'
    Delete this component.

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_location(self)
    Get this component's (x,y) location

Returns:
    Tuple[int, int]: The x,y location of the component, in grid units

  Method: get_parameters(self)
    

  Method: get_size(self) -> 'Tuple[int, int]'
    

  Method: glyphs(fget)
    Show/hide curve glyphs

  Method: grid(fget)
    Show/hide grid

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: markers(fget)
    Show/hide X/O markers

  Method: minimize(self, flag: 'bool' = True) -> 'None'
    Minimize the frame.

  Method: parameters(self, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    parameters(parameter=value, ...)
Get/set Plot Frame Settings

.. table:: Plot Frame Settings

   ============== ====== ===========================================
   Param Name     Type   Description
   ============== ====== ===========================================
   title          Text   Caption
   glyphs         Bool   Show Glyphs
   ticks          Bool   Show Ticks
   grid           Bool   Show Grid
   grid_color     Color  Grid Line Colour
   curve_colours  Colors Curve Colour Pattern
   curve_colours2 Colors Curve Inverted Colour Pattern
   yinter         Bool   Show Y-Intercept
   xinter         Bool   Show X-Intercept
   markers        Choice Show Markers: HIDE, SHOW
   xmarker        Real   X Marker
   omarker        Real   O Marker
   ============== ====== ===========================================

  Method: parent(fget)
    Canvas this component is located on (read-only)

:type: Canvas

  Method: polar(fget)
    Enable/disable polar mode

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: restore(self) -> 'None'
    Restore the frame from its minimized state.

  Method: set_location(self, x, y)
    Set the component's (x,y) location

Parameters:
    x (int): The new x location for this component
    y (int): The new y location for this component

  Method: set_parameters(self, **parameters)
    

  Method: set_size(self, width: 'int', height: 'int') -> 'None'
    

  Method: show_crosshair(self, show: 'bool' = True) -> 'None'
    Set the crosshair visibility on or off.

  Method: show_glyphs(self, show: 'bool' = True) -> 'None'
    Set the curve glyph visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the curve glyphs.

  Method: show_grid(self, show: 'bool' = True) -> 'None'
    Set the grid visibility

Parameters:
    show (bool): Set to ``False`` to turn off the grid.

  Method: show_ticks(self, show: 'bool' = True) -> 'None'
    Set the tick visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the tick markers.

  Method: show_x_intercept(self, show: 'bool' = True)
    Set the X intercept visibility on or off

  Method: show_y_intercept(self, show: 'bool' = True) -> 'None'
    Set the Y intercept visibility on or off

  Method: size(fget)
    Set/get width & height of a sizeable component

  Method: ticks(fget)
    Show/hide axis tick marks

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: toggle_crosshair(self) -> 'None'
    Toggle crosshair on or off.

  Method: toggle_curve_glyphs(self) -> 'None'
    Toggle curve glyphs on or off

  Method: toggle_grid_lines(self) -> 'None'
    Toggle grid lines on or off

  Method: toggle_minimize(self) -> 'None'
    Toggle minimized/restored state

  Method: toggle_tick_marks(self) -> 'None'
    Toggle tick marks on or off

  Method: toggle_x_intercept(self) -> 'None'
    Toggle X intercept on or off

  Method: toggle_y_intercept(self) -> 'None'
    Toggle Y intercept on or off

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component

  Method: x_intercept(fget)
    Show/hide x-intercept

  Method: y_intercept(fget)
    Show/hide y-intercept

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, ymin: 'Optional[float]' = None, ymax: 'Optional[float]' = None, start: 'Optional[float]' = None, end: 'Optional[float]' = None) -> 'None'
    Set the horizontal, vertical, and/or aperture limits of the XY Plot.

All parameters are optional.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit
    start (float): Aperture start
    end (float): Aperture end

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Class: PolyGraph
  Polygraph

A graph object that may be contained in a graph frame, used to display
strips of analog and digital traces.

Inherits from :class:`.GraphPanel`.

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self, *curves)
    

  Method: copy_data_all(self) -> 'None'
    Copy all data in the graph to the clipboard.

  Method: copy_data_between_markers(self) -> 'None'
    Copy data between the markers in the graph to the clipboard.

  Method: copy_data_visible(self) -> 'None'
    Copy visible data in the graph to the clipboard.

  Method: create_curve(self, source: 'Union[PGB, Curve]', index: 'int' = -1, digital: 'bool' = False) -> 'Curve'
    Create a new curve on this polygraph from the given source.

If the source is another curve, that curve is cloned.

Parameters:
    source: The signal source (pgb or a curve)
    index: Position in graph (optional)
    digital: Whether curve should be displayed as a digital signal (optional)

Returns:
    Curve: The created curve

  Method: create_curves(self, *sources: 'Union[PGB, Curve]', digital: 'bool' = False) -> 'List[Curve]'
    Create new curves on this polygraph from the given sources.

If a source is another curve, that curve is cloned.

Parameters:
    *sources: The signal sources (pgbs or curves)
    digital: Whether curve should be displayed as a digital signal (optional)

Returns:
    List[Curve]: The created curves

  Method: crosshair(fget)
    Show / hide crosshair

  Method: curve(self, index: 'int') -> 'Curve'
    Retrieve a curve that belongs to this graph panel.

Parameters:
    index (int): The curve index, starting from 0

Returns:
    Curve: the indexed curve in this panel

  Method: curves(self) -> 'List[Curve]'
    Retrieve the curves that belong to this graph panel.

Returns:
    List[Curve]: the curves in this panel

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self, *curves)
    

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_parameters(self)
    

  Method: glyphs(fget)
    Show / hide curve glyphs

  Method: grid(fget)
    Show / hide grid

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: move_curve(self, curve: 'Curve', index: 'int' = -1)
    Move a curve to a given position on this graph.

If a curve is on a different graph, it is removed from that graph,
and added to this one.

Parameters:
    curve: The curve to move.
    index: Position in graph (Optional)

  Method: move_down(self) -> 'None'
    Move the graph one position down in the graph frame.

  Method: move_up(self) -> 'None'
    Move the graph one position up in the graph frame.

  Method: parameters(self, *, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: paste(self)
    Paste contents of the clipboard

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: set_parameters(self, **parameters)
    

  Method: show_crosshair(self, show: 'bool' = True) -> 'None'
    Set the crosshair visibility on or off.

  Method: show_glyphs(self, show: 'bool' = True) -> 'None'
    Set the curve glyph visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the curve glyphs.

  Method: show_grid(self, show: 'bool' = True) -> 'None'
    Set the grid visibility

Parameters:
    show (bool): Set to ``False`` to turn off the grid.

  Method: show_ticks(self, show: 'bool' = True) -> 'None'
    Set the tick visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the tick markers.

  Method: show_x_intercept(self, show: 'bool' = True)
    Set the X intercept visibility on or off

  Method: show_y_intercept(self, show: 'bool' = True) -> 'None'
    Set the Y intercept visibility on or off

  Method: ticks(fget)
    Show / hide axis tick marks

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_bottom(self) -> 'None'
    Put at the bottom of the graph frame.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: to_top(self) -> 'None'
    Put at the top of the graph frame.

  Method: toggle_crosshair(self) -> 'None'
    Toggle crosshair on or off.

  Method: toggle_curve_glyphs(self) -> 'None'
    Toggle curve glyphs on or off

  Method: toggle_grid_lines(self) -> 'None'
    Toggle grid lines on or off

  Method: toggle_tick_marks(self) -> 'None'
    Toggle tick marks on or off

  Method: toggle_x_intercept(self) -> 'None'
    Toggle X intercept on or off

  Method: toggle_y_intercept(self) -> 'None'
    Toggle Y intercept on or off

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component

  Method: x_intercept(fget)
    Show / hide x-intercept

  Method: y_intercept(fget)
    Show / hide y-intercept

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, ymin: 'Optional[float]' = None, ymax: 'Optional[float]' = None, *, compute_x_grid: 'bool' = True, compute_y_grid: 'bool' = True) -> 'None'
    Set the horizontal and/or vertical limits of the overlay graph.

All parameters are optional.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit
    compute_x_grid (bool): Recompute x-grid spacing
    compute_x_grid (bool): Recompute y-grid spacing

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Class: PreferencesMixin
  Grid line, tick marks, glyphs, and X/Y intercept preferences

  Method: glyphs(fget)
    Show/hide curve glyphs

  Method: grid(fget)
    Show/hide grid

  Method: show_glyphs(self, show: 'bool' = True) -> 'None'
    Set the curve glyph visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the curve glyphs.

  Method: show_grid(self, show: 'bool' = True) -> 'None'
    Set the grid visibility

Parameters:
    show (bool): Set to ``False`` to turn off the grid.

  Method: show_ticks(self, show: 'bool' = True) -> 'None'
    Set the tick visibility.

Parameters:
    show (bool): Set to ``False`` to turn off the tick markers.

  Method: show_x_intercept(self, show: 'bool' = True)
    Set the X intercept visibility on or off

  Method: show_y_intercept(self, show: 'bool' = True) -> 'None'
    Set the Y intercept visibility on or off

  Method: ticks(fget)
    Show/hide axis tick marks

  Method: toggle_curve_glyphs(self) -> 'None'
    Toggle curve glyphs on or off

  Method: toggle_grid_lines(self) -> 'None'
    Toggle grid lines on or off

  Method: toggle_tick_marks(self) -> 'None'
    Toggle tick marks on or off

  Method: toggle_x_intercept(self) -> 'None'
    Toggle X intercept on or off

  Method: toggle_y_intercept(self) -> 'None'
    Toggle Y intercept on or off

  Method: x_intercept(fget)
    Show/hide x-intercept

  Method: y_intercept(fget)
    Show/hide y-intercept


Class: SizeableMixin
  Things that can be resized

  Method: get_size(self) -> 'Tuple[int, int]'
    

  Method: set_size(self, width: 'int', height: 'int') -> 'None'
    

  Method: size(fget)
    Set/get width & height of a sizeable component


Class: ZComponent
  All ZSLibrary components

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_parameters(self)
    

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: parameters(self, *, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: set_parameters(self, **parameters)
    

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component


Class: ZFrame
  Frames, which are movable, sizeable, and have a "title" parameter

  Method: add_to_layer(self, name: 'str') -> 'None'
    Add this component to the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to add the component to.

  Method: clone(self, x: 'int', y: 'int') -> 'Component'
    Copy this component and place the copy at the given location.

Parameters:
    x (int): x-coordinate for the cloned component (in grid units)
    y (int): y-coordinate for the cloned component (in grid units)

Returns:
    Component: the cloned component

  Method: command(self, cmd_name)
    

  Method: copy(self) -> 'bool'
    Copy this component to the clipboard.

  Method: custom_state(self, state_name: 'str', state: 'str') -> 'None'
    Set a custom layer state for this component.

The component must already be part of a layer, and that layer must
already have the named custom state :meth:`added <.Layer.add_state>`
to it.  This component will be set to the given state when the
component's layer is set to the given custom state name.

Parameters:
    state_name (str): The component's layer's custom state name
    state (str): One of 'Enabled', 'Disabled' or 'Invisible'

  Method: cut(self) -> 'bool'
    Cut this component to the clipboard

  Method: delete(self) -> 'bool'
    Delete this component.

  Method: disable(self) -> 'None'
    Disable this component.

This component will be disabled regardless of the layer states.
To re-enable this component use the :meth:`.enable` function.

  Method: enable(self, enable: 'bool' = True) -> 'None'
    Enable this component.

With no argument, or if given a `True` value, this will enable a
disabled component.  If the component is disabled via layers it will
remain disabled.

Parameters:
    enable (bool): If set to `False`, disables the component (optional)

  Method: enabled(fget)
    The component's enable/disable status.

This is independent of whether the component is on a layer that is enabled or disabled.

.. versionadded:: 2.8.1

  Method: export_parameters(self, csvfile: 'str') -> 'None'
    Export component parameters to a CSV file

Write component parameters to a two-line CSV file.
The first row will contains the parameter names and the second row
will contain the parameter values.
The first column contains the component identifier and the
parameter name will be blank.

  Method: get_location(self)
    Get this component's (x,y) location

Returns:
    Tuple[int, int]: The x,y location of the component, in grid units

  Method: get_parameters(self)
    

  Method: get_size(self) -> 'Tuple[int, int]'
    

  Method: import_parameters(self, csvfile: 'str') -> 'None'
    Import component parameters from a CSV file

Read component parameters from a two-line CSV file,
where the first row contains the parameter names and the second row
contains the parameter values.  The first column represents the
component identifier and the parameter name must be blank.

  Method: layer(fget)
    The layer the component is on

  Method: minimize(self, flag: 'bool' = True) -> 'None'
    Minimize the frame.

  Method: parameters(self, *, parameters: 'Optional[Parameters]' = None, **kwargs) -> 'Optional[Parameters]'
    Set or get the component's parameters.

Parameters:
    parameters (dict): A dictionary of parameter values. (optional)
    **kwargs: name=value keyword parameters.

Returns:
    A dictionary of parameter values (if no parameters are being set),
    or None.

  Method: parent(fget)
    Canvas this component is located on (read-only)

:type: Canvas

  Method: project(self) -> 'Project'
    The project this component exists in

:type: Project

  Method: range(self, parameter: 'str') -> 'ParameterRange'
    Get legal values for a parameter

Parameters:
    parameter (str): A component parameter name

Returns:
    * a ``tuple``, or ``frozenset`` of legal values, or
    * a ``range`` of legal values (integer setttings only), or
    * a ``Tuple[float, float]`` defining minimum & maximum values, or
    * an exception if the parameter does not have a defined range.

  Method: remove_from_layer(self, name: 'str') -> 'None'
    Remove this component from the given layer.

The layer must exist, but need not be enabled or visible.

Parameters:
    name (str): The layer to remove the component from.

  Method: restore(self) -> 'None'
    Restore the frame from its minimized state.

  Method: set_location(self, x, y)
    Set the component's (x,y) location

Parameters:
    x (int): The new x location for this component
    y (int): The new y location for this component

  Method: set_parameters(self, **parameters)
    

  Method: set_size(self, width: 'int', height: 'int') -> 'None'
    

  Method: size(fget)
    Set/get width & height of a sizeable component

  Method: to_back(self) -> 'bool'
    Put at the start (back) of the Z-Order on the canvas.

  Method: to_front(self) -> 'bool'
    Put at the front (end) of the Z-Order on the canvas.

  Method: to_next(self) -> 'bool'
    Move the component one position forward in the Z-Order relative to the
current Z-Order position.

  Method: to_prev(self) -> 'bool'
    Move the component one position backward in the Z-Order relative to the current
current Z-Order position.

  Method: toggle_minimize(self) -> 'None'
    Toggle minimized/restored state

  Method: view_parameter_grid(self) -> 'bool'
    View the parameter grid for this component


Class: ZoomMixin
  Zoom X/Y/In/Out/Extents/Limits/Previous/Next

  Method: reset_extents(self) -> 'None'
    Reset the graph's extents

  Method: reset_limits(self) -> 'None'
    Reset the graph's limits

  Method: zoom(self, xmin: 'Optional[float]' = None, xmax: 'Optional[float]' = None, ymin: 'Optional[float]' = None, ymax: 'Optional[float]' = None, *, compute_x_grid: 'bool' = True, compute_y_grid: 'bool' = True) -> 'None'
    Set the horizontal and/or vertical limits of the overlay graph.

All parameters are optional.

Parameters:
    xmin (float): Lower X-Axis limit
    xmax (float): Upper X-Axis limit
    ymin (float): Lower Y-Axis limit
    ymax (float): Upper Y-Axis limit
    compute_x_grid (bool): Recompute x-grid spacing
    compute_x_grid (bool): Recompute y-grid spacing

  Method: zoom_extents(self, x_extents: 'bool' = True, y_extents: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y extents.
By default, both X and Y axis zoom is affected.

Parameters:
    x_extents (bool): set to False to not affect X-axis
    y_extents (bool): set to False to not affect Y-axis

  Method: zoom_in(self) -> 'None'
    Increase the graph's zoom level.

  Method: zoom_limits(self, x_limits: 'bool' = True, y_limits: 'bool' = True) -> 'None'
    Reset the graph's zoom to the X and/or Y limits.
By default, both X and Y axis zoom is affected.

Parameters:
    x_limits (bool): set to False to not affect X-axis
    y_limits (bool): set to False to not affect Y-axis

  Method: zoom_next(self) -> 'None'
    Redo zoom.

  Method: zoom_out(self) -> 'None'
    Decrease the graph's zoom level.

  Method: zoom_previous(self) -> 'None'
    Undo zoom.

  Method: zoom_x_extents(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X extents.

  Method: zoom_x_limits(self) -> 'None'
    Reset the graph's zoom for the X-axis to the X limits.

  Method: zoom_y_extents(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y extents.

  Method: zoom_y_limits(self) -> 'None'
    Reset the graph's zoom for the Y-axis to the Y limits.


Function: deprecated(message='This method is deprecated')
  Flag a method as deprecated


Function: overload(func)
  Decorator for overloaded functions/methods.

In a stub file, place two or more stub definitions for the same
function in a row, each decorated with @overload.

For example::

    @overload
    def utf8(value: None) -> None: ...
    @overload
    def utf8(value: bytes) -> bytes: ...
    @overload
    def utf8(value: str) -> bytes: ...

In a non-stub file (i.e. a regular .py file), do the same but
follow it with an implementation.  The implementation should *not*
be decorated with @overload::

    @overload
    def utf8(value: None) -> None: ...
    @overload
    def utf8(value: bytes) -> bytes: ...
    @overload
    def utf8(value: str) -> bytes: ...
    def utf8(value):
        ...  # implementation goes here

The overloads for a function can be retrieved at runtime using the
get_overloads() function.


Function: rmi(_method=None, *, fallback=False)
  Remote Method Invocation

Applying this decorator to a method of a :class:`.Remotable` object causes
the method invocation to be forwarded to the remote application object.

If the remote application object's method does not exist and ``fallback``
is ``True``, then the body of the decorated method is used as an client-side
(and likely slower) implementation of the remote method.  Otherwise,
the body of the decorated method is ignored.


Class: rmi_property
  A property which is stored in a remote object

Apply this decorator to a property of a :class:`.Remotable` object causes
the property access attempts to be forwarded to the remote application
object.

Remote properties may never be deleted.

  Method: deleter(self, object, /)
    Descriptor to obtain a copy of the property with a different deleter.

  Method: getter(self, object, /)
    Descriptor to obtain a copy of the property with a different getter.

  Method: setter(self, object, /)
    Descriptor to obtain a copy of the property with a different setter.
